<Window x:Class="AudioScheduler.PlayTts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioScheduler"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Play TTS Sound"
        Height="250"
        Width="325">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="Enter the text you would like to convert to speech."
                   Margin="10" />
        <TextBox Name="TextBox"
                 TextWrapping="Wrap"
                 Height="75"
                 Margin="10" />
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Select Voice:"
                       Margin="10" />
            <ComboBox Name="Voices"
                      Width="200"
                      Margin="10" />
        </StackPanel>
        <UniformGrid Columns="2">
            <Button Content="Cancel"
                    Click="Cancel"
                    Margin="10" />
            <Button Content="Play"
                    Click="Play"
                    Margin="10" />
        </UniformGrid>
    </StackPanel>
</Window>